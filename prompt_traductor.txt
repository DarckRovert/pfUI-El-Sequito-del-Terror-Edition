necesitamos agregarle una funcion de traduccion universal para el addon pfUI, para que asi, por ejemplo cuando alguien este en el reino europeo pero no hable ingles, pueda ver los mensajes del chat que le llegan de manera automatica en el idioma en el que tenga puesto el cliente del juego, y sus mensajes de igual manera sean traducidos al ingles, (o al idioma del cliente del receptor) de ser posible, que quienes tengan tambien el addon pero no hablen ingles, pero por ejemplo si ruso o digamos chino, lo lean en su idioma y no en ingles, que seria como una traduccion por defecto. Debes utilizar la conexion con el proceso local (SuperWoW) o el modo de lectura externa para que la IA interactue. Devuelve el codigo Lua 5.0 correspondiente.
